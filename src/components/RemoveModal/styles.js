/**
 * Container
 * Styled Component que modifica a estilização do icone que representa a remoção
 * de um item.
 */
import styled from 'styled-components';

export const Container = styled.div`
  svg {
    transform: rotate(45deg);
  }
`;
